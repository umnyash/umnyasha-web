extends ../../layout/layout

block variables
  - var title = 'Свойство list-style-position'
  - var pageClass = 'article-page'

block content
  main
    article.article.container
      h1= title
      p Свойство #[b list-style-position] задаёт позицию маркера, относительно элемента списка.
      p Значения:
      ul
        li #[b outside] — (по умолчанию) маркер располагается вне элемента.
        li #[b inside] — маркер располагается внутри.

extends ../../layout/layout

block variables
  - var title = 'Метод lineTo.'
  - var pageClass = 'article-page'

block content
  main
    article.article.container
      h1= title
      p Метод #[b lineTo(x, y)] — добавить прямую линию от текущей позиции до (#[b x], #[b y]).
